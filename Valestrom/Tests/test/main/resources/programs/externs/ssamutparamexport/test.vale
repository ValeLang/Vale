export [<mut> 5 * int] as MutIntArray;

fn sumBytes(arr *[<mut> 5 * int]) int extern;
fn getMutIntArrayLen(arr *[<mut> 5 * int]) int export { len(&arr) }
fn getMutIntArrayElem(arr *[<mut> 5 * int], i int) int export { arr[i] }

fn main() int export {
  a = [mut 5]({_});
  = sumBytes(*a);
}
