import stdlib.*;

exported func main() int {
  i = 0;
  total = 0;
  while (i < 1000) {
    s = "Vale " + "ARM64";
    if (s != "Vale ARM64") { return 2; }
    set total = total + len(s);
    set i = i + 1;
  }
  if (total != 10000) { return 1; }
  return 0;
}
