export []<mut>int as MutIntArray;

fn sumBytes(arr *[]<mut>int) int extern;
fn getMutIntArrayLen(arr *[]<mut>int) int export { len(&arr) }
fn getMutIntArrayElem(arr *[]<mut>int, i int) int export { arr[i] }

fn main() int export {
  a = Array<mut, int>(5, {_});
  ret sumBytes(*a);
}
