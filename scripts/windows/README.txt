Vale Compiler, version 0.2
http://vale.dev/

To run a program:
  valec.exe build [options] [module directory mappings]

Run `valec.exe help build` for more.

Example:
  valec.exe build --output_dir target hello=./samples/helloworld
  target\main.exe
