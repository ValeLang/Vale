struct Spaceship export imm {
  fuel int;
  wings int;
}

export Array<imm, final, Spaceship> as ImmSpaceshipArray;

fn sumWings(arr Array<imm, final, Spaceship>) int extern;

fn main() int export {
  a = [imm *](5, (i){ Spaceship(i, i * 2) });
  = sumWings(a);
}
