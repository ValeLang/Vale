
import v.builtins.tup.*;

interface Car {
  func doCivicDance(virtual this Car) int;
}

struct Civic {}
impl Car for Civic;
func doCivicDance(civic Civic impl Car) int {
  ret 4;
}

struct Toyota {}
impl Car for Toyota;
func doCivicDance(toyota Toyota impl Car) int {
  ret 7;
}

exported func main() int {
  x Car = Toyota();
  ret doCivicDance(x);
}
