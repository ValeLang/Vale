
export []<mut>int as MutIntArray;

fn valeMakeRSA() []<mut>int export {
  ssa = #[#][5, 7, 9, 10, 11];
  ret Array<mut, int>(ssa.len(), { ssa[_] });
}

fn cMakeRSA() []<mut>int extern;

fn main() int export {
  arr = cMakeRSA();
  i = 0;
  total = 0;
  while (i < arr.len()) {
    set total = total + arr[i];
    set i = i + 1;
  }
  ret total;
}
