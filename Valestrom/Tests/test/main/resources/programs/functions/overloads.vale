
import v.builtins.tup.*;
import v.builtins.arith.*;
import v.builtins.str.*;

func ~(a int, b int) infer-ret { ret +(a, b); }
func ~(a str, b str) infer-ret { ret +(a, b); }
exported func main() infer-ret { ret 3 ~ 3; }
