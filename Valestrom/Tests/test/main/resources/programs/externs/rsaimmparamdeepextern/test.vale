struct Spaceship export imm {
  fuel int;
  wings int;
}

export Array<imm, Spaceship> as ImmSpaceshipArray;

fn cSumFuel_vasp(arr Array<imm, Spaceship>) int extern;

fn main() int export {
  a = [imm *](5, (i){ Spaceship(i * 2, 7) });
  = cSumFuel_vasp(a);
}
