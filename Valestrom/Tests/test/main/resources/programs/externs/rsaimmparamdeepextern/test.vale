struct Spaceship export imm {
  fuel int;
  wings int;
}

export []<imm>Spaceship as ImmSpaceshipArray;

fn cSumFuel_vasp(arr []<imm>Spaceship) int extern;

fn main() int export {
  a = #[](5, (i) => { Spaceship(i * 2, 7) });
  ret cSumFuel_vasp(a);
}
